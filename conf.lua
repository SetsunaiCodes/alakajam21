function love.conf(t)

    t.version = "11.5"
    t.console = true
    t.window.width = 640       -- Game window base width (NEVER CHANGE THAT VALUE) - upscale in main instead
    t.window.height = 360      -- Game window base height (NEVER CHANGE THAT VALUE) - upscale in main instead
    t.window.vsync = 0

end